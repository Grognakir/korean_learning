# Контекст сессии — Korean Learning

Последнее обновление: 2026-08-07

## Чем занимаемся

Начали фазу 1 разработки. Итерация F1-I01 завершена: репозиторий и версии инструментов подготовлены к инициализации Next.js; само приложение и зависимости ещё не создавались.

## Принятые решения

- Пользователь разрешил все изменения в рамках утверждённого итеративного плана, включая замену старого неотслеживаемого `package.json` и локальные Git-коммиты.
- Проект фиксирует Node.js 24.18.0 LTS через `.nvmrc` и engines.
- Проект фиксирует pnpm 10.34.5: это актуальная ветка pnpm 10, явно поддерживаемая Vercel; версия проверена через Corepack.
- `package.json` приватный, имеет версию приложения `0.1.0` и пока не содержит зависимостей или scripts — их создаст F1-I02.
- `.gitignore` закрывает секреты, dependencies, Next.js build, test artifacts, локальные Supabase/Vercel данные и `.obsidian`.
- `PLAN_REVISION_2026-08-07.md` является пользовательским неотслеживаемым файлом и не включается в коммит F1-I01.

## Открытые задачи

- [ ] Выполнить F1-I02 в отдельной итерации: активировать Node.js 24.18.0 и инициализировать Next.js App Router без Tailwind в корне.
- [ ] Установить только зависимости, необходимые F1-I02, и создать `pnpm-lock.yaml`.
- [ ] Проверить dev server, typecheck и production build.
- [ ] Не изменять и не коммитить `PLAN_REVISION_2026-08-07.md` без отдельной необходимости.

## Контекст для следующей сессии

Рабочая ветка — `chore/environment-and-repository`. F1-I01 использует `.nvmrc` 24.18.0 и `packageManager: pnpm@10.34.5`; локальный системный `node` пока 22.15.0, а доступный глобальный/fallback `pnpm` — 11.16.0, поэтому команды проекта нужно выполнять закреплённым pnpm через Corepack и после активации Node.js 24.18.0. В staging должны входить только `.gitignore`, `.nvmrc`, `package.json`, `APPLICATION_PLAN.md` и `.Codex/sessions/current.md`; `.obsidian` игнорируется, `PLAN_REVISION_2026-08-07.md` остаётся вне коммита.
